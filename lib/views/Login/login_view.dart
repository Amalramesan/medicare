import 'package:flutter/material.dart';
import 'package:med_care/views/Login/Widgets/login_widget.dart';

class LoginPage extends StatelessWidget {
  LoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Logindetails());
  }
}
